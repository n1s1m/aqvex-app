# React + TypeScript + Vite + shadcn/ui

This is a template for a new Vite project with React, TypeScript, and shadcn/ui.

## Adding components

To add components to your app, run the following command:

```bash
npx shadcn@latest add button
```

This will place the ui components in the `src/components` directory.

## Using components

To use the components in your app, import them as follows:

```tsx
import { Button } from "@/components/ui/button"
```

Тестове завдання:

"Вітаю, дякую за відгук. Оскільки кандидатів дуже багато, пропоную одразу перейти до тестового завдання, яке передбачає верстання сторінки каталогу товарів за макетом: Figma (https://www.figma.com/design/CMy9bnx2VGTVsj7uSBFf9Y/aptitude-test)
Для отримання даних, потрібно використати ендпоінт:
https://ip-194-99-21-145-139178.vps.hosted-by-mvps.net/api/v1/products

Обов'язкові технології для використання: TypeScript + React + Vite.
Базово очікується реалізація візуально наближеної версти до макета із відображенням списку товарів, пошуком за назвою товару, сортуванням, пагінацією. Деталі реалізації залишаються на ваш розсуд.
Допускається і бажано використання ШІ як допоміжного інструменту, але важливо, щоб Ви добре розуміли власне рішення та могли пояснити логіку реалізації.

Очікуємо на виконання, до 2-х днів, результат потрібно завантажити на GitHub та надати доступ користувачу montythora: https://github.com/montythora

Дякую!"


live demo on vercel
https://aqvex-app.vercel.app/